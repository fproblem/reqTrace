# Зависимости
node_modules/

# Байткод Python (тесты бэка гоняются с volume-монтированием — pyc сыплются в рабочую копию)
__pycache__/

# Секреты — только локально, образец в reqtrace/.env.example
.env

# Артефакты сборки фронтенда
reqtrace/frontend/build/

# Локальные бэкапы (см. CLAUDE.md — не коммитить)
backups/

# macOS
.DS_Store
